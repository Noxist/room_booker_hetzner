# App Icon Assets

Place your app icon files in this folder. Suggested formats:

- `app.ico` (preferred for Windows builds)
- `app.png`
- `app.svg`
- `app.jpg`

The build script and GitHub Actions workflow will automatically use `app.ico` if present, otherwise they will fall back to a PNG.
